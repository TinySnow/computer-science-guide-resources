# 配图制作规范

## 适用范围

- 本项目保存计算机科学极简入门指南的图片资源。
- 正文新图片放在 `computer-science-guide/content/`。
- 旧版本放在 `computer-science-guide/deprecated/` 下对应日期目录。
- 图片文件名避免使用 `?`、`#`、`/` 等会影响 URL 解析的字符。

## 画布与布局

- `content/` 的现有图片是整套指南的唯一视觉基线。新图必须像同一套教程中的页面，而不是套用通用商业信息图、科技海报或 AI 插画风格。
- 默认画布为横向 16:9 教学信息图；现有基准尺寸为 `1672 × 941`。可以因内容改用横向叙事图、流程图、结构图、时间线或对照图，但先确认竖向或非 16:9 构图确实能提升理解。
- 白色或略带冷色的近白背景占画面绝大部分；不要使用深色底、照片背景、强纹理、霓虹光效或装饰性渐变背景。背景只可使用极淡的蓝灰色块或极淡的圆形衬底来划分区域。
- 画布四周保留稳定的安全边距。以 `1672 × 941` 为例，左右约 45–65 px、上方约 30–45 px、下方约 25–40 px；所有文字、边框、箭头端点和图标都必须位于安全区内。
- 默认采用从上到下的阅读层次：顶部为标题和一句解释，中部为主图或主流程，底部为结论。内容复杂时可分为左—中—右三栏，或按 3–5 个步骤横向展开；读者必须一眼看出从哪里开始、如何阅读、最后得到什么结论。
- 适合概念教学的默认结构是“标题 + 简短导语 + 主图/步骤 + 底部关键点条”；这不是每张图的机械模板。叙事图应让人物、工具和结果形成因果关系，精确结构图应优先保证连线与分区清楚。
- 一个画面只讲一个核心问题或一个紧密相连的推导。删去重复的说明、无助于理解的图例和装饰；当信息过多时拆成两张图，不要把字、卡片和箭头无限缩小。
- 主要流程优先从左到右；层级、执行顺序或数据流可从上到下。箭头应连接因果、转换、输入输出或层级关系，不能只为装饰而出现。箭头两端必须明显，交叉不可造成归属歧义。
- 对比图使用并列、同尺度、同层级的区域；流程图使用等距的节点与箭头；表格的列和行必须严格对齐。不要把不相关的布局逻辑混在一张图里。
- 文字、插图、连线和边框必须互不遮挡；所有元素都要在画布可见范围内，并留出与内容尺度相称的边距。小型插图中的文字只在有足够空间时使用；否则改用更短标签或移到插图外。

### 版式组件

- 标题位于左上或顶部居中，但同一张图只能选择一种。标题为画面中最大的文字；标题下用一行简短导语回答“这张图要解释什么”。不要在标题区堆叠段落。
- 信息区以圆角卡片、轻量分组边框和留白组织。常规卡片使用白色或极淡的语义色填充、1–3 px 的细色边框、约 14–24 px 圆角；不使用厚重投影、玻璃拟态、立体浮雕或高对比描边。
- 卡片标题可放在顶部的浅色圆角标签中；正文、例子、表格和示意图在其下方整齐排列。一个卡片内最多突出一个主结论。
- 外层分组可使用细实线或蓝色虚线边框；虚线用于“整体范围”“补充说明”或“结论区”，不应与所有卡片都同时使用。
- 底部总结条是重要组件：常用白底、蓝色虚线或细蓝边框，左侧配简洁灯泡图标，随后是“关键点”“重要提示”等短标签和 1–2 句结论。它应总结主图，而不是引入新概念。
- 可使用圆形编号（1、2、3……）标识步骤；编号与步骤标题使用同一语义色。步骤数量通常控制在 3–5 个，超过时按阶段分组。

## 文件格式

- 不强制使用 SVG。根据图片性质选择格式：可编辑的电路图、逻辑图和精确结构图可使用 SVG；叙事插图、人物场景或 SVG 难以画好的内容优先使用 PNG 等位图格式。
- 不要为了使用 SVG 而牺牲构图、比例、可读性或审美。
- 不同图片可以使用不同尺寸和格式；Markdown 只引用最终确认的单张资源，不把多张独立图片拼成一张。

## 文字排版

- 中文字体使用清晰的无衬线系统字体，例如 PingFang SC、Microsoft YaHei 或 Noto Sans CJK；全图保持同一字体族和一致的字重层级。不要混用手写体、展示字体、衬线正文字体或难以渲染的网络字体。
- 主标题使用深海军蓝、粗体，约为正文的 1.8–2.5 倍；副标题/导语使用深蓝或蓝灰色，字重较轻。卡片标题、步骤名和“关键点”标签使用粗体；正文保持常规字重。
- 以 `1672 × 941` 为例：主标题通常约 48–64 px，导语约 24–32 px，区块标题约 28–38 px，正文约 20–28 px，最小辅助文字不小于约 18 px。缩放画布时按比例放大，不以牺牲正文可读性换取更多内容。
- 正文按语义换行，每行保持短而完整；不要把一个句子拆成难以连续阅读的碎片。中文、英文、数字与符号的间距要自然，避免英文或数字贴住中文。
- 数字、二进制串、变量、布尔表达式、单位和公式必须清晰、准确且便于核对。公式可使用与正文协调的数学/衬线字体，技术标签可用无衬线或等宽字体；同类符号在整图中保持相同写法。
- 表格的表头、单元格内容、分隔线和数值必须对齐；数值的位数、上下标、负号、小数点和进制下标不可被裁切或用颜色掩盖。不要仅靠浅色背景区分表头和数据行，还要有边线或位置层级。
- SVG 的 `<text>` 不会自动换行。长句必须主动拆成多个 `<text>` 或 `<tspan>`，不能依赖浏览器自动处理。
- 估算宽度时，中文字符按约一个字号计算，等宽字体、字距和粗体还要额外留余量。
- 文字放入小卡片前，先按实际字体测量或渲染预览；不要只根据字符数量判断是否放得下。
- 不要用 `<text font-size="..." class="formula">` 试图覆盖类中的字体简写。需要小字号时定义独立类，例如 `.formula-small`。
- 长标题、正文和底部总结分别预留独立区域；如果一行接近边界，优先分行，不要只把字号压得很小。
- 数学公式和英文标签使用等宽字体时，必须单独检查其宽度，不能沿用中文正文的宽度估算。
- SVG 图片应包含简短、准确的 `<title>` 和 `<desc>`。

## 颜色

- 主题色是深海军蓝与中蓝，例如 `#102a43`、`#0f285c`、`#1f63c7`、`#3478c5`。标题、正文、主要线稿、箭头和关键边框均以此蓝色体系为骨架。
- 辅助色使用低饱和浅底色配较深文字/边框，而非大面积纯色填充。现有基准包括：蓝色（基础概念、主流程）、绿色（稳定状态、对应关系、结论）、橙色（转换、比较、需注意的计算）、紫色（另一分支、抽象模块或输出）和少量红色（真正的警示或高优先级结果）。
- 每张图选择 2–4 种语义色即可；同一语义在整张图中保持颜色一致。色彩用于帮助分组和追踪流程，不能代替标题、箭头、标签或表格位置。
- 色块保持浅、边框和文字保持深，保证浅色屏幕和打印时仍有对比度。禁止大面积纯黑、纯红、荧光色、彩虹渐变和无语义的多色装饰。
- 电路图中不同信号、路径或状态可以使用不同辅助色；同一语义在整张图中保持颜色一致。
- 不要仅依靠颜色表达关键信息，必要时同时使用文字、线型或形状区分。

## 图形语言与技术图

- 图标和插图使用简洁的深海军蓝线稿：线宽稳定、圆角端点/圆角连接、形状易辨识。人物、电脑、纸张、芯片、灯泡、键盘、硬盘等可以有少量浅色填充和淡色圆形衬底，但不画成写实照片、3D 渲染或动漫角色。
- 真实世界的例子应服务于概念：例如用人、纸张和机器说明自动化，用键盘、内存、显示器说明输入存储输出。图标需统一视角、线宽和复杂度，不能混入不同来源的图标库样式。
- 电路图必须遵守标准符号和信号方向：输入通常在左、输出在右；导线横平竖直、少折返；节点、反相小圆圈、接地、电源、端口名称和高低电平都要清楚。交叉导线只有在确实连接时才使用节点标记。
- 对真值表、表达式到电路的推导，使用同一变量名、相同的 0/1 含义和可追踪的颜色/高亮。仅突出当前步骤，不要同时高亮所有内容。
- 用“前后对照”讲转换时，前后对象保持相近的尺寸和位置，并以明显箭头连接；用“逐步推导”讲过程时，每一步都应能从上一步自然得到。
- 逻辑、算术、编码、单位换算等内容必须先由人工复核事实、例子和公式，再生成图。教育图首先要正确，画面漂亮不能抵消概念错误。

## 内容与可读性要求

- 标题使用读者的问题或结论导向的表述，例如“为什么……？”“从……到……”或“……的关系”；导语直接说明机制，不写空泛宣传语。
- 把解释压缩为短句、短列表和可见的例子。新术语第一次出现时给出中文名、必要的英文缩写和简短含义；后文统一使用同一个名称。
- 重点信息通过位置、字号、粗体、色块、编号或边框共同突出；不要只把文字加粗或改色。每个区块有清楚的标题，每条箭头有清楚的起点和终点。
- 关键结论放在图的最末阅读位置，并能脱离正文被理解。结论不要简单重复标题，应回答该机制为何重要、如何使用或容易误解什么。
- 图片在博客正文的常见阅读宽度下必须仍能读清正文、表头、变量和公式。若需要放大才可读，改为减少信息、拆图或增大字，不要依赖读者缩放。

## 检查流程

- 修改后先运行 `xmllint --noout <file.svg>` 检查 XML 合法性。
- 使用对应格式的渲染器生成预览，在完整画布和实际阅读尺寸下检查文字溢出、重叠、裁切、比例和错位。SVG 可用 `sips -s format png <file.svg> --out <preview.png>` 生成完整比例预览。
- 预览时必须检查画布四周、文字密集区、连线交汇处和插图边缘；缩略图不能代替完整画布检查。对 PNG 也必须实际查看完整图，确认没有生成模型常见的错字、乱码、公式错误、断裂连线、重复图标或不可读的小字。
- 按本规范逐项复核：标题和导语是否清楚；主流程是否有明确方向；颜色是否有稳定语义；卡片、表格和箭头是否对齐；底部关键点是否总结主图；画面是否仍属于白底、深蓝线稿、低饱和辅助色这一套视觉系统。
- 对涉及技术事实的图，逐项对照正文核验术语、变量、单位、位数、进制、公式、真值表和电路连接；对涉及人物或设备的图，核验图标是否准确表达预期角色或部件。
- 一章的图片和 Markdown 修改完成后，再统一运行 `mdbook build` 并检查构建结果。
- 图片有问题时在本地修正，不要先推送有问题的中间版本。

## 提交节奏

- 同一章节内可以连续生成和修改多张图片，不必每张图片都提交或推送。
- 以章节完成为提交节点：图片、Markdown 引用和构建检查全部通过后，再分别提交并推送资源仓库和博客仓库。

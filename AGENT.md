# 配图制作规范

## 适用范围

- 本项目保存计算机科学极简入门指南的图片资源。
- 正文新图片放在 `computer-science-guide/content/`。
- 旧版本放在 `computer-science-guide/deprecated/` 下对应日期目录。
- 图片文件名避免使用 `?`、`#`、`/` 等会影响 URL 解析的字符。

## 画布与布局

- 默认使用 `1740x1040` 画布和 `viewBox="0 0 1740 1040"`，保持与现有配图一致。
- 内容安全区默认使用 x=90 到 x=1650；文字不得贴近画布边缘。
- 卡片内文字至少保留 40px 的水平内边距和 32px 的垂直内边距。
- 文字不得越过卡片边界，也不得覆盖其他文字、图例、连线或插图。
- 卡片底部最后一行的文字基线至少距离卡片底部 24px，并为字体下伸部分留出空间。
- 小型插图中的文字只在有足够空间时使用；否则改用更短标签或移到插图外。

## 文字排版

- SVG 的 `<text>` 不会自动换行。长句必须主动拆成多个 `<text>` 或 `<tspan>`，不能依赖浏览器自动处理。
- 估算宽度时，中文字符按约一个字号计算，等宽字体、字距和粗体还要额外留余量。
- 文字放入小卡片前，先按实际字体测量或渲染预览；不要只根据字符数量判断是否放得下。
- 不要用 `<text font-size="..." class="formula">` 试图覆盖类中的字体简写。需要小字号时定义独立类，例如 `.formula-small`。
- 长标题、正文和底部总结分别预留独立区域；如果一行接近边界，优先分行，不要只把字号压得很小。
- 数学公式和英文标签使用等宽字体时，必须单独检查其宽度，不能沿用中文正文的宽度估算。
- 所有图片都应包含简短、准确的 `<title>` 和 `<desc>`。

## 颜色

- 主题色保持深海军蓝和蓝色体系，例如 `#102a43`、`#3478c5`。
- 辅助色不限制为橙色，可根据语义使用绿色、青色、紫色、红色等，但要保持克制并保证文字与背景的对比度。
- 电路图中不同信号、路径或状态可以使用不同辅助色；同一语义在整张图中保持颜色一致。
- 不要仅依靠颜色表达关键信息，必要时同时使用文字、线型或形状区分。

## 检查流程

- 修改后先运行 `xmllint --noout <file.svg>` 检查 XML 合法性。
- 使用浏览器或 SVG 渲染器生成预览，在完整画布和实际阅读尺寸下检查文字溢出、重叠、裁切和错位。
- 预览时必须查看画布最右侧、最底部以及所有小卡片；缩略图不能代替完整画布检查。
- 一章的图片和 Markdown 修改完成后，再统一运行 `mdbook build` 并检查构建结果。
- 图片有问题时在本地修正，不要先推送有问题的中间版本。

## 提交节奏

- 同一章节内可以连续生成和修改多张图片，不必每张图片都提交或推送。
- 以章节完成为提交节点：图片、Markdown 引用和构建检查全部通过后，再分别提交并推送资源仓库和博客仓库。
